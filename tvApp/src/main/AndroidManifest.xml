<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Offline media cache: dual-target downloads (internal + USB).
         Legacy: broad storage read (<=32) / write (<=28).
         Modern: MANAGE_EXTERNAL_STORAGE (30+) gives the TV "All files
         access" so the engine can read/write encrypted bundles at
         /storage/<UUID>/NovelReadCache/ on FAT32/exFAT/NTFS USB drives
         (same special-access model Kodi uses on Android TV). Media-permission
         fallbacks keep scanning working on 33+. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Required for in-app updates: lets the TV app download the new APK and
         hand it to the Android package installer. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- TV requirement: declare TV hardware features -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />

    <!-- hardwareAccelerated is REQUIRED on Android TV: WebView HTML5 video
         (VidLink/VidSrc/MultiEmbed/AutoEmbed/EmbedSu) renders a black surface
         when the app runs software-rendered. The embed page loads and shows
         player chrome, but the <video> surface never draws without GPU accel. -->
    <application
        android:allowBackup="true"
        android:extractNativeLibs="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:banner="@drawable/tv_banner"
        android:icon="@drawable/ic_tv_launcher"
        android:roundIcon="@drawable/ic_tv_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.NovelAppTV"
        android:supportsRtl="true"
        android:hardwareAccelerated="true">

        <activity
            android:name=".TvMainActivity"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden"
            android:exported="true"
            android:screenOrientation="landscape">

            <!-- LEANBACK launcher — shows app on TV home screen -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- In-app updates: lets the TV app download the new APK and hand it
             to the Android package installer. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
