services:
  - type: web
    name: novelapp
    runtime: static
    buildCommand: echo "Publishing NovelApp static site"
    staticPublishPath: ./site
    autoDeployTrigger: commit
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
